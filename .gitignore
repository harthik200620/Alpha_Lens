*.pyc
__pycache__/
venv/
.alpha-venv/
.tmp/
.uv-cache/
.uv-python/
*.log
*.db
*.db-shm
*.db-wal
tmp_*
perf.txt
backtest_results.json
news_dataset_mini.csv
verification_log.json
temp_report.txt
news_cache.db-shm
news_cache.db-wal
news_cache.db
backend\news_cache.db
C:\Users\vella\OneDrive\Desktop\New folder (2)\Alpha_Lens\news_cache.db
.env
.vscode/
*.backup

# Claude Code personal/local config — holds API keys (e.g. CONTEXT7_API_KEY)
# and personal permission allowlists. Keep out of git. Shared config (hooks,
# team permissions) lives in .claude/settings.json instead.
.claude/settings.local.json

# Context7 MCP server config holds the real CONTEXT7_API_KEY inline. Keep local.
.mcp.json

# Stray notebooks / one-off artifacts / binders — keep out of git.
*.ipynb
Alpha_Lens_Pro_Desk_Features.pdf
# Business/strategy docs — kept local like the features PDF (repo may be public).
MARKET_RESEARCH.md
ALPHA_LENS_PRO_DESK_FEATURES.md
MVP_VALIDATION_PLAN.md

# Calibration / eval analysis scratch — one-off scripts + generated data/plots.
# The production logic lives in backend/eval_loop.py + signals/calibration.py.
scratch/

# Skill eval workspace — generated benchmark runs + static viewer HTML. The
# meaningful artifacts (eval prompts, assertions, benchmark.md) live committed
# under .claude/skills/honest-review/evals/. Keep the bulky generated tree local.
.claude/skills/*-workspace/
