"""Persistence layer — SQLite/Postgres connection wrappers, the write-with-retry
helper, the Postgres pool, and schema builders.

Modules: db (connections, db_write, pool), schema (init_db / init_news_db).
"""
